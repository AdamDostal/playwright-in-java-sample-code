# Test Environment Configuration
base.url=https://practicesoftwaretesting.com
api.base.url=https://api.practicesoftwaretesting.com

# Page URLs
login.url=${base.url}/auth/login
register.url=${base.url}/auth/register
contact.url=${base.url}/contact
products.url=${base.url}/products
cart.url=${base.url}/checkout

# API Endpoints
api.users.url=${api.base.url}/users
api.products.url=${api.base.url}/products
api.login.url=${api.base.url}/users/login

# Test Configuration
browser.headless=false
browser.timeout=30000
screenshot.on.failure=true